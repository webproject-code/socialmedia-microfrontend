import TopbarForMobile from '../components/TopbarForMobile';

const Chat: React.FC = () => {
  return (
    <div>
      <TopbarForMobile title="Messages" />
      Chat
    </div>
  );
};

export default Chat;
